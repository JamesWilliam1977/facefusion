interface:
  display_name: "Facefusion"
  short_description: "Repo-specific patterns and workflows for facefusion"
  default_prompt: "Use the facefusion repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true